[project]
name = "motif"
version = "1.0.0"
description = "Motif: mouse and keyboard recorder, auto clicker, and macro recorder. Tested and proven on macOS."
readme = "README.md"
requires-python = ">=3.11"
keywords = [
    "motif",
    "auto-clicker",
    "autoclicker",
    "auto clicker",
    "mac auto clicker",
    "mouse-recorder",
    "keyboard-recorder",
    "macro-recorder",
    "click recorder",
    "mouse macro",
    "input-recorder",
    "input-replay",
    "macro replay",
    "tinytask",
    "replay",
    "automation",
    "pynput",
    "macos",
]
dependencies = [
    "PySide6>=6.6.0",
    "pynput>=1.7.7",
    "mss>=9.0.0",
    "Pillow>=10.0.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
motif = "motif.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["motif*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
